# kb-scrapper-uploader

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run ./scr/app.js
```

https://github.com/amerkurev/scrapper/tree/master

docker run -d -p 3010:3000 --name scrapper amerkurev/scrapper:latest


